# ========================================
# AISIS Credentials
# ========================================
AISIS_USERNAME=your_aisis_username
AISIS_PASSWORD=your_aisis_password

# ========================================
# Supabase Configuration
# ========================================
SUPABASE_URL=https://your-project-id.supabase.co
DATA_INGEST_TOKEN=your_edge_function_auth_token

# Multi-university support: identifies which university's data is being scraped
# This is injected into all records and metadata to scope deletions correctly
# Default: ADMU (Ateneo de Manila University)
# Other values: ADDU (Ateneo de Davao University)
# UNIVERSITY_CODE=ADMU

# ========================================
# Google Sheets Configuration (Optional)
# ========================================
# GOOGLE_SERVICE_ACCOUNT={"type":"service_account",...}
# SPREADSHEET_ID=your_spreadsheet_id

# ========================================
# Scraper Configuration
# ========================================
# Optional: Override the academic term (skips auto-detection)
# Examples: 2025-1, 2025-2, 2025-3
# AISIS_TERM=2025-1

# Legacy support (AISIS_TERM takes precedence)
# APPLICABLE_PERIOD=2025-1

# ========================================
# Performance Tuning
# ========================================
# Number of records to send per HTTP request to Supabase
# Default: 2000
# Range: 500-5000
# SUPABASE_CLIENT_BATCH_SIZE=2000

# ========================================
# Schedule Scraper Performance Options
# ========================================
# Enable fast mode: skips term detection when AISIS_TERM is set,
# skips test department pass, and uses optimized delays
# Default: false
# FAST_MODE=true

# Number of departments to scrape in parallel per batch
# Default: 8
# Range: 1-20
# AISIS_CONCURRENCY=8

# Delay in milliseconds between department batches
# Default: 500ms
# Range: 0-5000ms (0 = no delay for maximum speed)
# AISIS_BATCH_DELAY_MS=500

# Filter which departments to scrape (comma-separated list)
# Useful for local development or testing specific departments
# Example: "DISCS,MA,EN,EC" (only scrape these departments)
# Default: all canonical departments if not specified
# AISIS_DEPARTMENTS=DISCS,MA,EN

# ========================================
# Curriculum Scraper Performance Options
# ========================================
# Limit number of curriculum versions to scrape (takes first N)
# Useful for local development or testing
# Example: "10" (scrape only first 10 programs)
# Default: all programs if not specified
# CURRICULUM_LIMIT=10

# Select specific curriculum versions to scrape (comma-separated)
# Takes precedence over CURRICULUM_LIMIT
# Example: "BS CS_2024_1,BS ME_2023_1"
# Default: all programs if not specified
# CURRICULUM_SAMPLE=BS CS_2024_1,BS ME_2023_1

# Delay in milliseconds between curriculum requests
# Default: 100ms (improved from 500ms for better performance)
# Range: 0-5000ms (0 = no delay for maximum speed)
# CURRICULUM_DELAY_MS=100

# Number of curriculum programs to scrape in parallel
# Default: 2 (improved from 1 for better performance - parallel scraping enabled!)
# Range: 1-5 (higher values = faster but more aggressive)
# CURRICULUM_CONCURRENCY=2

# ========================================
# Baseline Tracking & Regression Detection
# ========================================
# Percentage drop threshold for regression alerts
# If current count is X% less than previous, trigger alert
# Default: 5.0 (5%)
# BASELINE_DROP_THRESHOLD=5.0

# Whether to only warn (not fail) when regression is detected
# true = log warning but job succeeds
# false = fail job when regression exceeds threshold
# Default: true
# BASELINE_WARN_ONLY=true
